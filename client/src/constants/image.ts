// Pixels
export const IMAGE_THUMBNAIL_MAX_DIMENSION = {
    width: 125,
    height: 125,
};

// Pixels
export const IMAGE_PREVIEW_MAX_DIMENSION = {
    width: 500,
    height: 500,
};

// Pixels
export const IMAGE_RESIZED_MAX_DIMENSION = {
    width: 1000,
    height: 800,
};

export enum CANVAS_USE_CASE {
    thumbnail,
    preview,
    resized
}

export const STORY_IMAGE_FILE_EXTENSION = 'png';

export const STORY_IMAGE_MIME_TYPE = 'image/png';

// Pixels (width and height are always the same)
export const GOOGLE_PHOTO_DIMENSION = 94;
